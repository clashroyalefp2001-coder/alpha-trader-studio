# 📊 Инструкция по запуску Alfa Trading Dashboard

## Обзор проекта

Проект состоит из двух частей:
1. **Python WebSocket сервер** - подключается к Alfa Terminal, получает данные и передает их в дашборд
2. **React дашборд** - красивый веб-интерфейс для отображения торговых данных в реальном времени

---

## 🔧 Предварительные требования

### 1. Python (для WebSocket сервера)
- Python 3.11 или выше
- Alfa Terminal должен быть **запущен** на порту 3366

### 2. Node.js (для React дашборда)
- Node.js 18 или выше
- npm или bun

---

## 📦 Установка

### Шаг 1: Настройка Python окружения

1. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

2. **Активируйте виртуальное окружение:**

**Windows:**
```bash
venv\Scripts\activate
```

**macOS/Linux:**
```bash
source venv/bin/activate
```

3. **Установите зависимости:**
```bash
pip install websockets pandas numpy scikit-learn python-dateutil
```

### Шаг 2: Настройка React дашборда

Зависимости уже установлены в проекте. Если нужно переустановить:
```bash
npm install
# или
bun install
```

---

## 🚀 Запуск приложения

### Шаг 1: Запустите Alfa Terminal
- **Важно!** Alfa Terminal должен быть запущен и доступен по адресу `ws://127.0.0.1:3366/router/`
- Войдите в терминал с вашими учетными данными

### Шаг 2: Запустите Python WebSocket сервер

1. **Убедитесь, что виртуальное окружение активировано**

2. **Запустите главный скрипт:**
```bash
python alfa_live_stream.py
```

Вы должны увидеть:
```
WebSocket сервер запущен на ws://127.0.0.1:8788
Подключение к Alfa Terminal...
Подключено к Alfa Terminal
```

### Шаг 3: Запустите React дашборд

В **новом терминале** (не закрывая Python сервер):

```bash
npm run dev
# или
bun run dev
```

Дашборд будет доступен по адресу: **http://localhost:8080**

---

## 🎯 Использование дашборда

### Основные функции:

1. **Выбор инструмента:**
   - Выберите тикер из выпадающего списка (SBER, GAZP, LKOH и т.д.)
   - Нажмите "Переключить" для смены инструмента

2. **Загрузка архивных данных:**
   - Выберите инструмент с пометкой "(архив)"
   - Нажмите "Загрузить архив"

3. **Настройка параметров стратегии:**
   - Измените значения параметров внизу дашборда
   - Нажмите "Применить параметры" для применения изменений

4. **Мониторинг в реальном времени:**
   - **Last** - текущая цена
   - **Score** - оценка сигнала
   - **Confidence** - уверенность в сигнале
   - **Brick** - размер кирпича Renko
   - **Next ↑/↓** - уровни следующих кирпичей
   - **LED индикаторы** - состояние RENKO, LR, MACD

5. **Статус торговли:**
   - **LONG** (зеленый) - длинная позиция
   - **SHORT** (красный) - короткая позиция
   - **FLAT** (серый) - вне рынка
   - **OFFLINE** - нет подключения

---

## 📁 Структура Python файлов

```
alfa_live_stream.py      - Главный скрипт WebSocket сервера
alfa_archive_loader.py   - Загрузка исторических данных
renko_builder.py         - Построение графика Renko
ticker_selector.py       - Управление выбором тикеров
period_selector.py       - Управление периодами
live_logger.py          - Логирование данных
```

---

## 🔍 Диагностика проблем

### Проблема: WebSocket сервер не запускается
**Решение:**
- Проверьте, что Alfa Terminal запущен
- Убедитесь, что порт 8788 свободен
- Проверьте, что все зависимости Python установлены

### Проблема: Дашборд показывает OFFLINE
**Решение:**
- Проверьте, что Python WebSocket сервер запущен
- Проверьте консоль браузера на наличие ошибок (F12)
- Убедитесь, что WebSocket подключается к `ws://127.0.0.1:8788`

### Проблема: Не приходят данные с Alfa Terminal
**Решение:**
- Проверьте, что Alfa Terminal запущен и вы вошли в систему
- Убедитесь, что терминал доступен по `ws://127.0.0.1:3366/router/`
- Проверьте логи Python сервера на наличие ошибок

### Проблема: Данные не отображаются в дашборде
**Решение:**
- Нажмите "Переключить" после выбора инструмента
- Проверьте статус подключения внизу панели управления
- Откройте консоль разработчика (F12) и проверьте вкладку Network → WS

---

## 🛠️ Расширенная настройка

### Настройка портов

**Python WebSocket сервер** (в `alfa_live_stream.py`):
```python
# Измените порт если необходимо
websockets.serve(handler, "127.0.0.1", 8788)
```

**React дашборд** (в `src/pages/Index.tsx`):
```typescript
// Измените порты если необходимо
const endpoints = ["ws://127.0.0.1:8788", "ws://localhost:8788"];
```

### Добавление новых инструментов

В файле `ticker_selector.py`:
```python
# Добавьте новые инструменты в список
instruments = [
    {"ticker": "SBER", "name": "Сбербанк", "has_archive": True},
    {"ticker": "GAZP", "name": "Газпром", "has_archive": False},
    {"ticker": "НОВЫЙ", "name": "Новый инструмент", "has_archive": False},
]
```

---

## 📊 Архитектура подключения

```
┌─────────────────┐         ┌──────────────────┐         ┌─────────────────┐
│  Alfa Terminal  │ ◄─────► │  Python Server   │ ◄─────► │  React Dashboard│
│   (Port 3366)   │   WS    │   (Port 8788)    │   WS    │   (Port 8080)   │
└─────────────────┘         └──────────────────┘         └─────────────────┘
       Данные                Обработка                    Отображение
```

**Поток данных:**
1. Alfa Terminal отправляет рыночные данные через WebSocket (порт 3366)
2. Python сервер получает данные, обрабатывает стратегию Renko
3. Python сервер отправляет обработанные данные через WebSocket (порт 8788)
4. React дашборд получает данные и отображает их в реальном времени

---

## 📝 Логи и отладка

### Python сервер
Логи выводятся в консоль. Для более детальных логов:
```python
# В alfa_live_stream.py добавьте
import logging
logging.basicConfig(level=logging.DEBUG)
```

### React дашборд
Откройте консоль разработчика (F12):
- **Console** - JavaScript логи и ошибки
- **Network → WS** - WebSocket сообщения
- **Elements** - DOM структура

---

## 🎨 Кастомизация дизайна

Все цвета и стили настраиваются через:
- `src/index.css` - основные CSS переменные
- `tailwind.config.ts` - конфигурация Tailwind CSS

Пример изменения цвета success (зеленый):
```css
/* src/index.css */
--success: 158 64% 52%;  /* HSL формат */
```

---

## ⚙️ Параметры стратегии

### Основные параметры:
- **hyst_k_lr** (2.0) - коэффициент гистерезиса для Linear Regression
- **hyst_k_macd** (3.0) - коэффициент гистерезиса для MACD
- **macd_strong_k** (1.0) - усиление сигнала MACD
- **threshold** (1.0) - порог принятия решения

### Веса индикаторов:
- **w_renko** (1.2) - вес Renko
- **w_lr** (0.8) - вес Linear Regression
- **w_macd** (0.8) - вес MACD
- **w_cross** (0.2) - вес пересечения
- **w_zero** (0.2) - вес нулевой линии

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте все пункты раздела "Диагностика проблем"
2. Убедитесь, что все сервисы запущены в правильном порядке
3. Проверьте логи Python сервера и консоль браузера
4. Убедитесь, что версии Python и Node.js соответствуют требованиям

---

## 🎉 Готово!

Ваш Alfa Trading Dashboard готов к использованию. Следите за рыночными данными в реальном времени с красивым и удобным интерфейсом!
